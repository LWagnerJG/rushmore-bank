# Local PartyKit (override the production default)
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999

# When unset, the app uses: rushmore-bank.lwagnerjg.partykit.dev

# Optional — enables OpenAI judging via /api/judge
# OPENAI_API_KEY=
